# Model-provider credential, bound only to the private Model Proxy Worker.
OPENCODE_API_KEY=

# Random shared secret for the private Sandbox runtime and scoped model grants.
SANDBOX_API_TOKEN=

# Agent-account GitHub credential used only by RepositoryPublisher after Pi exits.
# It must be able to create public forks, branches, commits, and draft pull requests.
GITHUB_TOKEN=
